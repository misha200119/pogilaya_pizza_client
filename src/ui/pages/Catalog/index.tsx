import React, { memo } from 'react';
import Pizza from '../../../utils/types/pizza';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const mockDataAvaliblePizza: Array<Pizza> = [];

export const Catalog = memo(() => {
  return (
    <div>Catalog</div>
  );
});
